<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Campus Bridge — Landing Page</title>

  <!-- Combined CSS -->
  <style>
    /* Reset & base */
    :root{
      --bg: #ffffff;
      --muted: #666;
      --accent1: #6a82fb;
      --accent2: #fc5c7d;
      --card: rgba(255,255,255,0.85);
      --shadow: 0 6px 30px rgba(10,10,20,0.08);
    }
    *{box-sizing:border-box;margin:0;padding:0}
    html,body{height:100%}
    body{
      font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
      background: linear-gradient(180deg,#f7f9ff 0%, #ffffff 100%);
      color:#1b1b1b;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      line-height:1.5;
    }

    /* Header */
    header{
      width:100%;
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:1rem;
      padding:14px 28px;
      position:sticky;
      top:0;
      background:rgba(255,255,255,0.9);
      backdrop-filter: blur(6px);
      box-shadow: 0 1px 0 rgba(0,0,0,0.03);
      z-index:40;
    }
    .logo{
      display:flex;
      align-items:center;
      gap:10px;
      font-weight:700;
      font-size:18px;
      color: #222;
    }
    .logo .mark{
      width:36px;height:36px;border-radius:8px;
      background: linear-gradient(135deg,var(--accent1),var(--accent2));
      display:inline-grid;place-items:center;color:white;font-weight:800;
      box-shadow: var(--shadow);
    }

    /* Nav */
    nav{
      display:flex;
      gap:18px;
      align-items:center;
    }
    nav a{
      text-decoration:none;
      color:var(--muted);
      font-weight:600;
      font-size:15px;
      padding:8px 10px;
      border-radius:8px;
    }
    nav a:hover{color:#111;background:rgba(0,0,0,0.04)}

    /* CTA on header */
    .cta{
      padding:8px 14px;
      background:linear-gradient(90deg,var(--accent1),var(--accent2));
      color:white;
      border-radius:10px;
      text-decoration:none;
      font-weight:700;
      box-shadow: 0 8px 24px rgba(108,99,255,0.12);
      margin-left:6px;
      font-size:14px;
    }

    /* Mobile menu icon */
    .menu-toggle{
      display:none;
      background:transparent;border:0;font-size:22px;cursor:pointer;
    }

    /* Hero */
    .hero{
      min-height:78vh;
      display:grid;
      place-items:center;
      padding:40px 20px;
      text-align:center;
      background: linear-gradient(135deg, rgba(106,130,251,0.12) 0%, rgba(252,92,125,0.08) 100%);
    }
    .hero-inner{
      max-width:980px;
      width:100%;
      padding:36px;
      border-radius:18px;
      background: linear-gradient(180deg, rgba(255,255,255,0.75), rgba(255,255,255,0.6));
      box-shadow: var(--shadow);
      display:grid;
      gap:18px;
      align-items:center;
    }
    .kicker{
      font-size:13px;
      font-weight:700;
      color:var(--muted);
      display:inline-block;
      padding:6px 10px;
      border-radius:999px;
      background: rgba(105,105,255,0.06);
      width:max-content;
    }
    h1{
      font-size:38px;
      line-height:1.05;
      color:#0b1220;
      margin-top:6px;
    }
    p.lead{
      color:#4b5563;
      font-size:17px;
      max-width:780px;
      margin:0 auto;
    }
    .hero-actions{
      display:flex;
      gap:12px;
      justify-content:center;
      margin-top:8px;
      flex-wrap:wrap;
    }
    .btn-primary{
      padding:12px 20px;border-radius:12px;font-weight:800;
      text-decoration:none;background:linear-gradient(90deg,var(--accent1),var(--accent2));
      color:white;box-shadow: 0 14px 40px rgba(108,99,255,0.12);
    }
    .btn-ghost{
      padding:12px 18px;border-radius:12px;font-weight:700;
      text-decoration:none;background:transparent;border:1px solid rgba(0,0,0,0.06);
      color:#1b1b1b;
    }

    /* Features grid (small extra section) */
    .features{
      display:grid;
      grid-template-columns:repeat(3,1fr);
      gap:18px;
      margin-top:20px;
      padding-top:6px;
    }
    .card{
      background:var(--card);
      border-radius:12px;
      padding:16px;
      box-shadow: 0 8px 30px rgba(15,15,30,0.04);
      text-align:left;
    }
    .card h4{margin-bottom:8px;font-size:16px}
    .card p{font-size:14px;color:var(--muted)}

    /* Footer */
    footer{
      margin-top:32px;
      padding:28px 22px;
      text-align:center;
      color:#666;
      font-size:14px;
    }
    .social{margin-top:12px;display:flex;gap:10px;justify-content:center;flex-wrap:wrap}
    .social a{color:var(--muted);text-decoration:none;font-weight:700;padding:6px 10px;border-radius:8px}
    .social a:hover{background:rgba(0,0,0,0.03);color:#111}

    /* Responsive */
    @media (max-width:1000px){
      .features{grid-template-columns:repeat(2,1fr)}
      h1{font-size:34px}
    }
    @media (max-width:760px){
      header{padding:12px 16px}
      nav{display:none}
      .menu-toggle{display:block}
      .hero-inner{padding:22px;border-radius:14px}
      h1{font-size:26px}
      .features{grid-template-columns:1fr}
      .logo{font-size:16px}
    }

    /* Slide-down mobile nav (simple) */
    .mobile-nav{
      position:fixed;
      right:16px;
      top:64px;
      background:white;
      border-radius:12px;
      box-shadow:0 12px 40px rgba(10,10,30,0.12);
      padding:10px;
      display:flex;
      flex-direction:column;
      gap:6px;
      z-index:60;
    }
    .mobile-nav a{padding:10px 12px;border-radius:8px;color:#111;text-decoration:none;font-weight:700}
    .mobile-nav a:hover{background:rgba(0,0,0,0.03)}
    .hidden{display:none}
  </style>
</head>
<body>

  <!-- Header -->
  <header>
    <div class="logo" aria-label="Campus Bridge logo">
      <div class="mark">CB</div>
      Campus Bridge
    </div>

    <nav aria-label="Main navigation">
      <a href="#">Home</a>
      <a href="#">Features</a>
      <a href="#">About</a>
      <a href="#">Contact</a>
    </nav>

    <div style="display:flex;align-items:center;gap:10px">
      <a href="#" class="cta" aria-label="Sign up">Sign up</a>
      <button class="menu-toggle" id="menuBtn" aria-expanded="false" aria-controls="mobileNav" title="Toggle menu">☰</button>
    </div>
  </header>

  <!-- Mobile nav (hidden by default) -->
  <div id="mobileNav" class="mobile-nav hidden" role="menu" aria-hidden="true">
    <a href="#">Home</a>
    <a href="#">Features</a>
    <a href="#">About</a>
    <a href="#">Contact</a>
    <a href="#" class="cta" style="display:block;text-align:center;margin-top:6px">Get Started</a>
  </div>

  <!-- Hero -->
  <main>
    <section class="hero" aria-labelledby="hero-heading">
      <div class="hero-inner">
        <div class="kicker">Launch faster • Learn together</div>
        <h1 id="hero-heading">Build skills, ship projects — welcome to Campus Bridge</h1>
        <p class="lead">
          Campus Bridge is a simple learning & coding platform concept — created to help students learn programming,
          collaborate on projects, and prepare for placements. Clean UI, responsive layout, and a friendly workflow.
        </p>

        <div class="hero-actions" role="group" aria-label="Primary actions">
          <a href="#" class="btn-primary">Get Started — It's Free</a>
          <a href="#" class="btn-ghost">Learn More</a>
        </div>

        <!-- Features grid -->
        <div class="features" aria-hidden="false" style="margin-top:18px">
          <div class="card">
            <h4>Interactive Coding</h4>
            <p>Practice problems, online compiler, and guided projects to help you learn hands-on.</p>
          </div>
          <div class="card">
            <h4>Structured Courses</h4>
            <p>Curated learning paths and assessments to measure progress clearly and efficiently.</p>
          </div>
          <div class="card">
            <h4>Placement Ready</h4>
            <p>Mock interviews, resume tools, and company-specific preparation workflows.</p>
          </div>
        </div>
      </div>
    </section>
  </main>

  <!-- Footer -->
  <footer>
    <div>© <span id="year"></span> Campus Bridge · All rights reserved</div>
    <div class="social" role="navigation" aria-label="Social links">
      <a href="#" aria-label="Facebook">Facebook</a>
      <a href="#" aria-label="Twitter">Twitter</a>
      <a href="#" aria-label="Instagram">Instagram</a>
    </div>
  </footer>

  <!-- Small JS for mobile nav and year -->
  <script>
    (function(){
      const btn = document.getElementById('menuBtn');
      const nav = document.getElementById('mobileNav');
      const yearEl = document.getElementById('year');
      yearEl.textContent = new Date().getFullYear();

      btn.addEventListener('click', () => {
        const isHidden = nav.classList.toggle('hidden');
        nav.setAttribute('aria-hidden', String(isHidden));
        const expanded = btn.getAttribute('aria-expanded') === 'true';
        btn.setAttribute('aria-expanded', String(!expanded));
      });

      // Close mobile nav on outside click
      document.addEventListener('click', (e) => {
        if (!nav.classList.contains('hidden') && !nav.contains(e.target) && !btn.contains(e.target)) {
          nav.classList.add('hidden');
          nav.setAttribute('aria-hidden', 'true');
          btn.setAttribute('aria-expanded', 'false');
        }
      });

      // Close on Escape
      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && !nav.classList.contains('hidden')) {
          nav.classList.add('hidden');
          nav.setAttribute('aria-hidden', 'true');
          btn.setAttribute('aria-expanded', 'false');
        }
      });
    })();
  </script>
</body>
</html>
